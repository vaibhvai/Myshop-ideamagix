import React from "react";

const Happy = () => {
  const textStyle = {
    textAlign: "center", // Align text to the center
    fontWeight: "bold", // Make text bold
  };

  return <h1 style={textStyle}>Happy Shopping!😄</h1>;
};

export default Happy;
